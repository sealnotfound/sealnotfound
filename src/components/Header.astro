---
import { getLangFromUrl, useTranslations, languages, langFlags, defaultLang, type Lang } from '@/i18n/index';

const lang = getLangFromUrl(Astro.url);
const t = useTranslations(lang);
const currentPath = Astro.url.pathname;

// Build URL for switching to a target language
function getLangUrl(targetLang: Lang): string {
  const allLangs = Object.keys(languages) as Lang[];
  // Detect if current path starts with a lang prefix
  const parts = currentPath.replace(/^\//, '').split('/');
  const hasPrefix = allLangs.includes(parts[0] as Lang) && parts[0] !== defaultLang;
  const pathWithoutLang = hasPrefix ? '/' + parts.slice(1).join('/') : currentPath;
  const cleanPath = pathWithoutLang === '' ? '/' : pathWithoutLang;

  if (targetLang === defaultLang) return cleanPath || '/';
  return `/${targetLang}${cleanPath === '/' ? '' : cleanPath}`;
}

const base = lang === defaultLang ? '' : `/${lang}`;

const navLinks = [
  { href: `${base}/`, label: t('nav.home') },
  { href: `${base}/blog`, label: t('nav.articles') },
  { href: `${base}/search`, label: t('nav.search') },
];
---

<header class="border-b border-zinc-200 dark:border-zinc-800 relative z-50">
  <!-- Flowers layer -->
  <div class="absolute inset-0 overflow-hidden pointer-events-none">
    <span class="absolute select-none" style="top:6px;left:3%;font-size:0.85rem;opacity:0.18;transform:rotate(-15deg)">🌸</span>
    <span class="absolute select-none" style="top:10px;left:9%;font-size:0.7rem;opacity:0.15;transform:rotate(10deg)">✦</span>
    <span class="absolute select-none" style="top:5px;left:16%;font-size:0.8rem;opacity:0.14;transform:rotate(-5deg)">🌷</span>
    <span class="absolute select-none" style="top:8px;left:24%;font-size:0.7rem;opacity:0.13;transform:rotate(20deg)">✿</span>
    <span class="absolute select-none" style="top:6px;left:32%;font-size:0.85rem;opacity:0.15;transform:rotate(-10deg)">🌸</span>
    <span class="absolute select-none" style="bottom:5px;left:12%;font-size:0.75rem;opacity:0.13;transform:rotate(8deg)">🌷</span>
    <span class="absolute select-none" style="bottom:6px;left:26%;font-size:0.7rem;opacity:0.12;transform:rotate(-12deg)">✦</span>
    <span class="absolute select-none" style="top:7px;right:32%;font-size:0.8rem;opacity:0.14;transform:rotate(5deg)">✿</span>
    <span class="absolute select-none" style="bottom:5px;right:28%;font-size:0.85rem;opacity:0.13;transform:rotate(-8deg)">🌸</span>
    <span class="absolute select-none" style="top:6px;right:20%;font-size:0.7rem;opacity:0.15;transform:rotate(15deg)">🌷</span>
    <span class="absolute select-none" style="bottom:7px;right:14%;font-size:0.75rem;opacity:0.12;transform:rotate(-5deg)">✦</span>
    <span class="absolute select-none" style="top:8px;right:7%;font-size:0.8rem;opacity:0.14;transform:rotate(12deg)">🌸</span>
  </div>

  <div class="relative max-w-3xl mx-auto px-4 h-14 flex items-center justify-between">
    <a href={`${base}/`} translate="no" class="notranslate font-mono font-medium text-amber-500 dark:text-amber-400 tracking-tight hover:text-amber-400 dark:hover:text-amber-300 transition-colors">
      Seal
    </a>

    <div class="flex items-center gap-3">
      <!-- Desktop nav -->
      <nav class="hidden sm:flex items-center gap-6">
        {navLinks.map(({ href, label }) => (
          <a href={href}
            class={`text-sm transition-colors ${
              currentPath === href || (href !== `${base}/` && currentPath.startsWith(href))
                ? 'text-zinc-900 dark:text-zinc-100'
                : 'text-zinc-500 dark:text-zinc-400 hover:text-zinc-900 dark:hover:text-zinc-100'
            }`}>
            {label}
          </a>
        ))}
      </nav>

      <!-- Language dropdown -->
      <div class="relative" id="lang-wrap">
        <button id="lang-btn" aria-label="Select language"
          class="flex items-center gap-1 text-zinc-500 dark:text-zinc-400 hover:text-zinc-900 dark:hover:text-zinc-100 transition-colors p-1 rounded-lg">
          <svg class="w-4 h-4" fill="none" stroke="currentColor" viewBox="0 0 24 24">
            <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M21 12a9 9 0 01-9 9m9-9a9 9 0 00-9-9m9 9H3m9 9a9 9 0 01-9-9m9 9c1.657 0 3-4.03 3-9s-1.343-9-3-9m0 18c-1.657 0-3-4.03-3-9s1.343-9 3-9m-9 9a9 9 0 019-9"/>
          </svg>
          <svg class="w-3 h-3 transition-transform duration-200" id="lang-chevron" fill="none" stroke="currentColor" viewBox="0 0 24 24">
            <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M19 9l-7 7-7-7"/>
          </svg>
        </button>

        <div id="lang-dropdown"
          class="hidden absolute right-0 top-full mt-1 bg-white dark:bg-zinc-900 border border-zinc-200 dark:border-zinc-800 rounded-xl shadow-lg overflow-hidden z-50 min-w-[155px]">
          {(Object.entries(languages) as [Lang, string][]).map(([code, label]) => (
            <a href={getLangUrl(code)}
              class={`flex items-center gap-2.5 px-4 py-2.5 text-sm transition-colors
                ${code === lang
                  ? 'font-semibold text-amber-500 dark:text-amber-400 bg-zinc-50 dark:bg-zinc-800'
                  : 'text-zinc-700 dark:text-zinc-300 hover:bg-zinc-50 dark:hover:bg-zinc-800'}`}>
              <span class="text-base">{langFlags[code]}</span>
              <span>{label}</span>
            </a>
          ))}
        </div>
      </div>

      <!-- Theme toggle -->
      <button id="theme-toggle" aria-label="Toggle theme"
        class="relative w-14 h-7 rounded-full bg-zinc-200 dark:bg-zinc-700 transition-colors duration-200 focus:outline-none">
        <span class="absolute top-1 left-1 w-5 h-5 rounded-full bg-white dark:bg-zinc-950 shadow transition-transform duration-200 dark:translate-x-7 flex items-center justify-center">
          <svg class="w-3 h-3 text-amber-500 dark:hidden" fill="none" stroke="currentColor" viewBox="0 0 24 24">
            <circle cx="12" cy="12" r="5" stroke-width="2"/>
            <line x1="12" y1="1" x2="12" y2="3" stroke-width="2" stroke-linecap="round"/>
            <line x1="12" y1="21" x2="12" y2="23" stroke-width="2" stroke-linecap="round"/>
            <line x1="4.22" y1="4.22" x2="5.64" y2="5.64" stroke-width="2" stroke-linecap="round"/>
            <line x1="18.36" y1="18.36" x2="19.78" y2="19.78" stroke-width="2" stroke-linecap="round"/>
            <line x1="1" y1="12" x2="3" y2="12" stroke-width="2" stroke-linecap="round"/>
            <line x1="21" y1="12" x2="23" y2="12" stroke-width="2" stroke-linecap="round"/>
            <line x1="4.22" y1="19.78" x2="5.64" y2="18.36" stroke-width="2" stroke-linecap="round"/>
            <line x1="18.36" y1="5.64" x2="19.78" y2="4.22" stroke-width="2" stroke-linecap="round"/>
          </svg>
          <svg class="w-3 h-3 text-amber-400 hidden dark:block" fill="none" stroke="currentColor" viewBox="0 0 24 24">
            <path d="M21 12.79A9 9 0 1 1 11.21 3 7 7 0 0 0 21 12.79z" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"/>
          </svg>
        </span>
      </button>

      <!-- Hamburger (mobile) -->
      <button id="menu-toggle" aria-label="Toggle menu"
        class="sm:hidden flex flex-col justify-center items-center w-8 h-8 gap-1.5 focus:outline-none">
        <span class="block w-5 h-0.5 bg-zinc-600 dark:bg-zinc-400 transition-all"></span>
        <span class="block w-5 h-0.5 bg-zinc-600 dark:bg-zinc-400 transition-all"></span>
        <span class="block w-5 h-0.5 bg-zinc-600 dark:bg-zinc-400 transition-all"></span>
      </button>
    </div>
  </div>

  <!-- Mobile menu -->
  <div id="mobile-menu" class="hidden sm:hidden border-t border-zinc-100 dark:border-zinc-800">
    <nav class="max-w-3xl mx-auto px-4 py-3 flex flex-col gap-1">
      {navLinks.map(({ href, label }) => (
        <a href={href}
          class={`py-2 text-sm transition-colors ${
            currentPath === href ? 'text-zinc-900 dark:text-zinc-100 font-medium' : 'text-zinc-500 dark:text-zinc-400'
          }`}>
          {label}
        </a>
      ))}
      <div class="flex items-center gap-2 pt-2 mt-1 border-t border-zinc-100 dark:border-zinc-800">
        {(Object.entries(languages) as [Lang, string][]).map(([code]) => (
          <a href={getLangUrl(code)}
            class={`px-2 py-1 rounded text-xs font-mono transition-colors uppercase
              ${code === lang ? 'text-amber-500 dark:text-amber-400 font-bold' : 'text-zinc-400 dark:text-zinc-600 hover:text-zinc-700 dark:hover:text-zinc-300'}`}>
            {langFlags[code]}
          </a>
        ))}
      </div>
    </nav>
  </div>
</header>

<script>
  document.getElementById('theme-toggle')?.addEventListener('click', () => {
    const isDark = document.documentElement.classList.toggle('dark');
    localStorage.setItem('theme', isDark ? 'dark' : 'light');
  });

  document.getElementById('menu-toggle')?.addEventListener('click', () => {
    const menu = document.getElementById('mobile-menu');
    const toggle = document.getElementById('menu-toggle');
    const opening = menu?.classList.contains('hidden');

    menu?.classList.toggle('hidden');

    if (toggle) {
      const spans = toggle.querySelectorAll('span');
      if (opening) {
        toggle.classList.replace('flex-col', 'flex-row');
        toggle.classList.replace('gap-1.5', 'gap-1');
        spans.forEach(s => { s.classList.replace('w-5', 'w-0.5'); s.classList.replace('h-0.5', 'h-5'); });
      } else {
        toggle.classList.replace('flex-row', 'flex-col');
        toggle.classList.replace('gap-1', 'gap-1.5');
        spans.forEach(s => { s.classList.replace('w-0.5', 'w-5'); s.classList.replace('h-5', 'h-0.5'); });
      }
    }
  });

  const langBtn = document.getElementById('lang-btn');
  const langDropdown = document.getElementById('lang-dropdown');
  const langChevron = document.getElementById('lang-chevron');

  langBtn?.addEventListener('click', e => {
    e.stopPropagation();
    langDropdown?.classList.toggle('hidden');
    langChevron?.classList.toggle('rotate-180');
  });

  document.addEventListener('click', () => {
    langDropdown?.classList.add('hidden');
    langChevron?.classList.remove('rotate-180');
  });
</script>
